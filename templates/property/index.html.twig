{% extends 'base.html.twig' %}
{% trans_default_domain '' %}
{% block title %}Hello PropertyController!{% endblock %}

{% block body %}
    {#
        <style>
            .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
            .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
        </style>

        <div class="example-wrapper">
            <h1>Hello {{ controller_name }}! ✅</h1>

            This friendly message is coming from:
            <ul>
                <li>Your controller at <code><a href="{{ '/home/romeo/Applications/MonSiteImmo/src/Controller/PropertyController.php'|file_link(0) }}">src/Controller/PropertyController.php</a></code></li>
                <li>Your template at <code><a href="{{ '/home/romeo/Applications/MonSiteImmo/templates/property/index.html.twig'|file_link(0) }}">templates/property/index.html.twig</a></code></li>
            </ul>
            {{ dump(property) }}
        </div>
    #}
<div class="container mt-4">
    <h1 style="display: inline;">Voir tous nos biens</h1>
    <div style="display: inline;">
        {{ form_start(formSearchProperty, {'attr': { 'action': path('property.index')  ,'role':'form', 'class':'form-inline'  }} ) }}
        <label>Rechercher un bien </label>&nbsp;&nbsp;
         {{ form_row(formSearchProperty.prix) }}
         {{ form_row(formSearchProperty.nbPiece) }}

        <button class="btn btn-primary" type="submit" >Rechercher</button>

        {{ form_end(formSearchProperty) }}
        <br>
    </div>
    <div class="row">
        {% for property in propertiesAvailable %}
            <div class="col-md-4">
                {% include 'property/_property.html.twig' %}
            </div>
        {% endfor %}
    </div>

    {# Ici on affiche la pagination càd ceci: «12345 » et pour personnaliser, il faut aller modifer #}
    {# Il faut aller modifier la configuration dans config/packages/knp_paginator.yaml #}
    <div class="navigation">
        {{ knp_pagination_render(pagination) }}
    </div>
</div>

{% endblock %}
